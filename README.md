# TestRepo

## SQL Job Scheduling

Use `sql_job_schedule.sql` to create a SQL Server Agent job, attach a schedule,
and trigger it immediately. The script is idempotent for the demo job name and
schedule name and can be customized by changing the job name, schedule, and
job step command.
