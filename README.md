# TestRepo

## .NET MediatR architecture examples

Code-side examples for MediatR in layered .NET architecture are available in:

- `examples/mediatr/`

That sample includes:

- Commands, queries, and handlers
- Pipeline behaviors (validation and logging)
- In-memory repository infrastructure implementation
- Minimal API endpoint wiring using `IMediator`
